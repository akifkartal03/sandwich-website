import React from 'react';
import { Container} from 'reactstrap';

const About = (props) => {
    return (
        <Container> <p>All recipes page will be shown here</p> </Container>
    );
};

export default About;